services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: shop
      POSTGRES_PASSWORD: local-development-only
      POSTGRES_DB: bigmamareps
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - shop-db:/var/lib/postgresql/data
volumes:
  shop-db:
